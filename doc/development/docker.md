## Commands to remember
1. docker compose down -v (Remove even the volumes)
2. docker volume ls (See the created volumes)
3. Starting the services - docker compose --env-file ../.env -f open-project-docker.yml up -d
4. Stopping the container - docker compose --env-file ../.env -f open-project-docker.yml down 